## Summary

<!-- What does this change do? Link the issue it closes, if any. -->

## Test plan

- [ ] `npm run typecheck` passes
- [ ] `npm run lint` passes
- [ ] `npm test` passes

## Manual verification

- [ ] Verified with the `in-memory` driver
- [ ] Verified with the `confluent` driver (Docker)

## Documentation

- [ ] I updated the relevant `docs/guides/*` page and the driver matrix
