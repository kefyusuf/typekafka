{
  "annotations": {
    "list": []
  },
  "editable": true,
  "fiscalYearStartMonth": 0,
  "graphTooltip": 0,
  "id": null,
  "links": [],
  "liveNow": false,
  "panels": [
    {
      "datasource": { "type": "prometheus", "uid": "nodejs-kafka-prometheus" },
      "fieldConfig": { "defaults": { "unit": "ops" } },
      "gridPos": { "h": 4, "w": 4, "x": 0, "y": 0 },
      "id": 1,
      "title": "Rate of messages produced",
      "type": "timeseries",
      "targets": [
        {
          "expr": "sum(rate(nodejs_kafka_messages_produced_total[1m])) by (topic)",
          "legendFormat": "{{topic}}"
        }
      ]
    },
    {
      "datasource": { "type": "prometheus", "uid": "nodejs-kafka-prometheus" },
      "fieldConfig": { "defaults": { "unit": "ops" } },
      "gridPos": { "h": 4, "w": 4, "x": 4, "y": 0 },
      "id": 2,
      "title": "Rate of messages consumed",
      "type": "timeseries",
      "targets": [
        {
          "expr": "sum(rate(nodejs_kafka_messages_consumed_total[1m])) by (topic)",
          "legendFormat": "{{topic}}"
        }
      ]
    },
    {
      "datasource": { "type": "prometheus", "uid": "nodejs-kafka-prometheus" },
      "fieldConfig": { "defaults": { "unit": "ops" } },
      "gridPos": { "h": 4, "w": 4, "x": 8, "y": 0 },
      "id": 3,
      "title": "Retries total",
      "type": "timeseries",
      "targets": [
        {
          "expr": "sum(rate(nodejs_kafka_retries_total[1m])) by (topic)",
          "legendFormat": "{{topic}}"
        }
      ]
    },
    {
      "datasource": { "type": "prometheus", "uid": "nodejs-kafka-prometheus" },
      "fieldConfig": { "defaults": { "unit": "ops" } },
      "gridPos": { "h": 4, "w": 4, "x": 0, "y": 4 },
      "id": 4,
      "title": "DLQ total",
      "type": "timeseries",
      "targets": [
        {
          "expr": "sum(rate(nodejs_kafka_dlq_total[1m])) by (topic)",
          "legendFormat": "{{topic}}"
        }
      ]
    },
    {
      "datasource": { "type": "prometheus", "uid": "nodejs-kafka-prometheus" },
      "fieldConfig": { "defaults": { "unit": "ops" } },
      "gridPos": { "h": 4, "w": 4, "x": 4, "y": 4 },
      "id": 5,
      "title": "Handler duration p95",
      "type": "timeseries",
      "targets": [
        {
          "expr": "histogram_quantile(0.95, sum(rate(nodejs_kafka_handler_duration_ms_bucket[1m])) by (le))",
          "legendFormat": "{{topic}}"
        }
      ]
    },
    {
      "datasource": { "type": "prometheus", "uid": "nodejs-kafka-prometheus" },
      "fieldConfig": { "defaults": { "unit": "ops" } },
      "gridPos": { "h": 4, "w": 4, "x": 8, "y": 4 },
      "id": 6,
      "title": "Outbox published",
      "type": "timeseries",
      "targets": [
        {
          "expr": "sum(rate(nodejs_kafka_outbox_published_total[1m])) by (topic)",
          "legendFormat": "{{topic}}"
        }
      ]
    }
  ],
  "refresh": "",
  "schemaVersion": 39,
  "tags": ["kafka", "nodejs"],
  "templating": {
    "list": []
  },
  "time": {
    "from": "now-6h",
    "to": "now"
  },
  "timepicker": {},
  "timezone": "browser",
  "title": "Node.js Kafka",
  "uid": "nodejs-kafka-observability",
  "version": 1,
  "weekStart": ""
}
